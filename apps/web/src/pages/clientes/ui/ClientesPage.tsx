import { EnConstruccion } from '@/shared/ui';

export function ClientesPage() {
  return (
    <EnConstruccion
      titulo="Clientes"
      detalle="La lista, la ficha y el alta de clientes llegan antes que Proyectos: un proyecto necesita su cliente."
    />
  );
}
