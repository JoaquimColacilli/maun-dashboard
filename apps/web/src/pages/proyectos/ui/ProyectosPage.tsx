import { EnConstruccion } from '@/shared/ui';

export function ProyectosPage() {
  return (
    <EnConstruccion
      titulo="Proyectos"
      detalle="La lista de proyectos, el detalle con pagos y gastos, y el cobro llegan en el paso que viene. Los datos ya están replicados en este dispositivo."
    />
  );
}
